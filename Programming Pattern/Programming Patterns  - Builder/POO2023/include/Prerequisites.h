#pragma once

#include <string>   // Inclusión de la librería estándar para manejo de cadenas de texto
#include <vector>   // Inclusión de la librería estándar para contenedores tipo vector
#include <iostream> // Inclusión de la librería estándar de entrada/salida
using namespace std; // Uso del espacio de nombres estándar para evitar prefijos

// La directiva "#pragma once" es un mecanismo para evitar la inclusión repetida de archivos de encabezado
// Asegura que el archivo de encabezado solo se incluya una vez durante la compilación

// Este archivo incluye algunas bibliotecas estándar comunes necesarias para la mayoría de programas C++